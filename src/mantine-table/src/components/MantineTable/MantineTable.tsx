import React, { forwardRef } from 'react';
// import { CompactTable, CompactTableProps } from '@table-library/react-table-library/compact';

export const MantineTable = forwardRef<HTMLDivElement, any>(
  (props, ref) => <div>asdasd</div>
);

MantineTable.displayName = '@mantine/table/MantineTable';
